#include <stdio.h>
/**
 * main- This is a special function that serve as the entry point program
 *
 * Return: It returns the execution of a funtion by returning zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
