#include "main.h"
/**
 * _abs - a function that solve the absolute value of the intege
 * @ab: integer input value
 * Return: absolute value of abs
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
